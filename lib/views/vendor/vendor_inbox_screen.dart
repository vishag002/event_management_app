import 'package:flutter/material.dart';

class VendorInboxScreen extends StatelessWidget {
  const VendorInboxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vendor Inbox Screen"),
      ),
    );
  }
}
