import 'package:flutter/material.dart';

class UserSelectCity extends StatelessWidget {
  const UserSelectCity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
